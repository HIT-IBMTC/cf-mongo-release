#!/usr/bin/env bash

package='cloudfoundry-mongodb-service-broker'
file="$package.tar.gz"
url="https://codeload.github.com/pivotal-education/$package/tar.gz/master"

if [ ! -s "$package/$package" ]; then
  mkdir -p "$package"
  wget -qO "$package/$file" --content-disposition "$url"
fi

exit 0
