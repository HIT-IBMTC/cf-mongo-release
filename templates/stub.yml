---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - sm_mongo
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: sm-mongo
    version: latest
